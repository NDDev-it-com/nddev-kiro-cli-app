# NDDev builder steering

NDDev builder work must preserve the public-module/private-harness boundary. Use current Kiro CLI native surfaces exactly as documented: user agents and skills under KIRO_HOME, steering markdown, v3 hooks JSON, v3 permissions YAML, and MCP JSON only when a real server is declared.

Do not invent marketplace or plugin formats for Kiro CLI.
